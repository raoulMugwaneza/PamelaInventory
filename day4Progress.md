Project context: I'm building "Pamoja" (Swahili for "together"), a learning project mirroring an earlier app I built called ShopInventory (Node/Express/SQLite backend, React frontend — products, categories, users, orders, approvals, stock counts). Pamoja is a from-scratch rebuild used purely to teach me software engineering fundamentals, one lesson at a time, using the same domain so concepts transfer directly.
Setup facts: Pamoja lives at ~/Documents/Code/Pamoja on my Mac, pushed to GitHub at https://github.com/raoulMugwaneza/PamelaInventory.git (repo name has a typo, "Pamela" instead of "Pamoja" — harmless, left as is). Server runs on port 3001 (not 3000, which is ShopInventory's). Stack so far: Express (backend), raw SQL practiced directly in the sqlite3 CLI against pamoja.db (not wired into Node yet). Git and GitHub are both already set up and working.
How to teach me: I write all the code myself, in my own terminal/editor — Claude does not have file access to my project and should not ask for it; I paste real code and real terminal/browser output back for review. Claude corrects precisely rather than vaguely, predicts-then-verifies before explaining, and holds me to pasting actual output rather than descriptions of expected behavior. Daily git commit after each concept is genuinely nailed, verified with real git log/push output before moving on. No sycophantic praise — direct, calibrated feedback, including telling me plainly when something's wrong. I ask a lot of side questions (npm vs node, git vs GitHub, destructuring, flags like -A/-m) — answer those directly when they come up, then return to the main task.
Target: Full, genuine understanding of CRUD (create/read/update/delete, in raw SQL and eventually in Express routes backed by a real database) by July 25, at ~4 hours/day. Depth over speed — if a day needs more time, take it; the date already slipped once from an original July 18 target specifically to protect real understanding over rushing.
Progress — Days 1-4, all complete, committed, and pushed to GitHub:
Day 1 — Client-server model, Express app.listen, why "Cannot GET" differs from connection-refused, npm init/install vs require(), .gitignore (why node_modules and *.db are excluded, with a concrete example of why committing node_modules breaks cross-platform), git init/add/commit, git vs GitHub distinction proven empirically with git push.
Day 2 — Plain app.get() routes, req/res, route params (req.params) and query strings (req.query), a subtle destructuring bug fixed, why route registration order matters (or doesn't) depending on Node's synchronous startup.
Day 3 — Restructured routes into express.Router() files, app.use(prefix, router) and the exact prefix-stripping mechanism (proved by deliberately breaking it first, then fixing it), module.exports vs local variable naming vs filename (three unrelated things).
Day 4 — SQL fundamentals: CREATE TABLE, primary keys, AUTOINCREMENT, INSERT, SELECT * vs selecting specific columns, WHERE filtering (including that a non-matching WHERE returns zero rows, not an error), the difference between a .sql file as a reference document versus an executable script. Saved as sql-notes/day4.sql.
Remaining plan:
Day 5 — UPDATE, DELETE, constraints (NOT NULL, UNIQUE).
Day 6 — Foreign keys and JOINs: design a categories-products relation, write the JOIN by hand.
Day 7 — SQLite meets Node: better-sqlite3, prepared statements, why they stop SQL injection, first real DB-backed route.
Day 8 — Full products CRUD route, proper error handling.
Day 9 — Transactions and multi-table writes: users, orders, order_items, db.transaction(), "place an order."
Day 10 — React fundamentals in isolation: components, JSX, props, state, useEffect.
Day 11 — Connect frontend to backend: fetch, async/await, CORS.
Day 12 — Second page with a form, form state. Stretch: sketch the approval/stock-count design.
Day 13 (target: July 25) — Checkpoint: cold-build exercise from memory, git review, honest read on what's left.